# ft_package

This is a "Hello World" package, from 42's school

## Features

- Provides a 'count_in_list' function that counts occurrences of an element in a list given.

## Installation

You can install the package via pip:

'''bash
pip install ./dist/ft_package-0.0.1.tar.gz
