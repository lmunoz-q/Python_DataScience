def count_in_list(lst, element):
    """
    Count the number of occurrences of a element in a list given.

    Args:
    -lst: list of elements
    -element: element that we want wo search
    -return: number of occurences of the element in lst
    """
    return lst.count(element)
